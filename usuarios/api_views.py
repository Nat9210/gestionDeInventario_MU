"""
Archivo creado temporalmente para pruebas de JMeter.
Se ha revertido su contenido. Si necesitas restaurarlo, revisa el historial de commits.
"""
from django.http import HttpResponse


def placeholder_api_views(request):
    return HttpResponse('Este archivo fue revertido y ya no contiene endpoints de prueba.', status=200)